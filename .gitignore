# Local dev tools (sync-upstream, etc.)
.claude/

# OS
.DS_Store
Thumbs.db

# Node
node_modules/

# open-designer test data (HTML drafts, init files generated while exercising the plugin locally)
.open-designer/

# Local clones of the plugins this marketplace points at. Nothing under here is
# tracked: marketplace.json fetches each plugin from its own GitHub repo, so a
# checkout here is a convenience for working on them side by side, never the
# source the marketplace serves.
plugins/

# Reference files
reference/

# Local planning docs
plans/

# Local browser-automation configs (machine-specific ports)
agent-browser.json
stagewise.json
